<div data-controller='edit-wpheader '>
  <div class='' data-edit-wpheader-target="infos">
    <div class='d-flex justify-content-between align-items-baseline' data-action="click->edit-challenge#displayForm">
      <h1><%= work_plan.name%><i class="fas fa-pen fa-xs ml-2 text-success"></i></h1>

      <center>
        <h4 class='date-domain'>Du <%= work_plan.start_date.strftime("%d/%m/%Y")%> Au <%= work_plan.end_date.strftime("%d/%m/%Y")%></h4>
      </center>
    </div>
  </div>
  <%# edit div for WP header %>
  <div class='edit-wp' data-edit-wphear-target="form">
    <%= simple_form_for work_plan, class: 'd-flex justify-content-between align-items-baseline' do |f|%>
      <p class='m-left'>Nom :</p>
      <%= f.input :name, label: false,
                      placeholder: 'Nom :',
                      input_html: { class: 'area  m-left' } %>
      <div class=" d-flex">
        <p class='m-left'>Du</p>
        <div class='edit-cal_wp'>
          <%= f.input :start_date, as: :string, label: false,
                          input_html: {class: 'datepicker area m-left ', locale: "fr"} %>
        </div>
        <p class='m-left'>Au</p>
        <div class='edit-cal_wp mr-2'>
          <%= f.input :end_date, as: :string, label: false,
                          input_html: {class: 'datepicker area m-left', locale: "fr"} %>
        </div>
      </div>
      <p class='m-left'>Elève :</p>
      <%= f.association :student, label: false,
                            collection: current_user.students.order(:first_name),
                            input_html: {class: 'area grow m-left'},
                            label_method: :first_name %>
      <%= f.submit "Valider", class: 'area rect-sm btn-valid',method: 'post', action: 'edit' %>
    <% end %>
  </div>
</div>
