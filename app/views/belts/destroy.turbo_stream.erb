<%= turbo_stream.replace "student_#{@student.id}domain_#{@domain.id}_level#{@level}" do %>
  <%= render "belts/belt", level: @level, domain: @domain, belt: nil, student: @student, skills: @skills,last_wps: @last_wps, results: @results %>
<% end %>
