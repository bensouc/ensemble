<%= turbo_stream.replace "general_modal" do%>
  ""
<%end%>
<%= turbo_stream.replace "student_#{@belt.student.id}domain_#{@belt.domain.id}_level#{@belt.level}" do%>
<%= render "belts/belt", level: @level, domain: @domain,
                        belt: @belt, student: @student,
                        last_belt: @last_belt,
                        skills: @skills, last_wps: @last_wps,
                        results: @results %>
<%end%>
